"""Per-system downloaders.

Each Imperial DoC system we want to pull data from (e.g. the Labs system,
department GitLab, Panopto recordings, personal web space, ...) gets its own
module here implementing one or more `imperial_doc_download.pipeline.Step`
subclasses. Nothing lives here yet — this is where that work goes next.
"""
